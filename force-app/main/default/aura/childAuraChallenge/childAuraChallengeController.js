({
    handleClick : function(component, event, helper) {
	    helper.handleClickHelper(component);
    },
    clearParentText : function(component, event, helper)
    {
        helper.handleClearParentText(component, event);
    }
})